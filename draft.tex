\begin{propose}
$$
\mbox{Conv}_{\ell}\intd{t_0}{t} f(\ell(s),s)ds=\intd{t_0}{t} \mbox{Conv}_{\ell(s)} f(\ell(s),s) ds
$$
\end{propose}

По определению выпуклой оболочки 
$$
\forall F_1(s),F_2(s)\in \mbox{Conv}\{\mathcal{F}\} \Rightarrow \alpha F_1(t)+(1-\alpha)F_2(t)\in \mbox(Conv)\{\mathcal{F}\}
$$
Пусть $F_1(s)=\intd{t_0}{t}f(\ell_1(s),s)ds$,  $F_2(s)=\intd{t_0}{t}f(\ell_2(s),s)ds$, $\ell_1(s),\ell_2(s)\in \mathcal{L}$.
Тогда 
$$
\alpha F_1(t)+(1-\alpha)F_2(t)=\alpha\intd{t_0}{t}f(\ell_1(s),s)ds+(1-\alpha)\intd{t_0}{t}f(\ell_2(s),s)ds=
$$
$$
=\intd{t_0}{t}\left[\alpha f(\ell_1(s),s)+(1-\alpha)f(\ell_2(s),s)\right]ds=\intd{t_0}{t}\mbox{Conv}_{\ell(s)}\{f(\ell(s),s)\}ds 
$$
{\centering $\blacksquare$}


Без умаления общности примем, что $\alpha=\ts{x(t_0),c}<\gamma$, тогда при $\alpha<0\;\Rightarrow t<\tau$ и при  $\alpha>0 \Rightarrow t>\tau$

%\cmnt{Далее я сталкиваюсь с проблемой вычисления conv. Получается, что если conv загонять под интеграл по ds, то остается неделимый $K(\ell,\mu)=\intd{w_0}{w}(\cdots)$. Если двигаться по пути разделения временных инервалов $T_{<},T_{>}$, есть вариант поменять местами $conv ( \intd{t_0}{t}\intd{w_0}{w}(\cdots)+K) = \intd{w_0}{w}conv( \intd{t_0}{t}(\cdots)+\ldots )$. Тогда есть надежда работать конкретно с каждыsм $\ell(w)$, а $\intd{t_0}{t}G_2(t_0,w)G_1(w,t)ds\rightarrow f(w)$, так как $G_1(s,t),G_2(s,t)$--известны, может быть, это будет разумно.  И если разделять по отрезкам времени, где $S(s,t)>0$, то делать это уже по $w$, а не по $s$ (там, где вычисляем conv). Сейчас хочу дописать, что получится. Другой вариант - вычисление сопряженной функции.}
  

\subsection{Пример 2D}
Заданы системы дифференциальных уравнений, описывающих состояние некоторой модели до переключения и после:
\begin{equation}
\left\{
\begin{aligned}
			%\begin{aligned}
			&\left[
			\begin{aligned}
			    &\left\{
					    \begin{aligned}
					        &\dot x_1^{(1)} = x_2^{(1)}+v^{(1)}(t);\\
					        &\dot x_2^{(1)} = u^{(1)}(t);\\
					        &x(t_0)\in\mathcal{X}_0=\{x\in \mathbb{R}^2:(x_1-2)^2+(x_2-1)^2\leqslant1\};\\
					    \end{aligned}
			    \right.\\
			    \\
			    &\left\{
					    \begin{aligned}
					        &\dot x_1^{(2)} = x_2^{(2)}+v^{(2)}(t);\\
					        &\dot x_2^{(2)} = -\gamma x_1 -\mu x_2 + u^{(2)}(t);\\
					        &x(\tau)\in\mathcal{X}^{(1)}[\tau,t_0]\cap\mathcal{H},\tau\leqslant t\leqslant t_1;
					    \end{aligned}
					\right.\\
			\end{aligned}
			\right.\\
			\\
			&\mathcal{H}=\{x\in \mathbb{R}^2: x_1=0\}\mbox{--- гиперплоскость};\\
			&u^{(1)}=u^{(2)}=u(\cdot)\in[-\alpha_1,\alpha_2]=\mathcal{P}[t_0,t_1];\\
			&v^{(1)}=v^{(2)}=v(\cdot)\in[-\beta_1,\beta_2]\in\mathcal{W}[t_0,t_1];\\
			&\gamma,\mu,\alpha_1,\alpha_2,\beta_1,\beta_2 > 0\mbox{--- некоторые константы};\\
			&\tau \mbox{ --- момент  переключения, при пересечении гиперплоскости}.\\
\end{aligned}
\right.
\label{eq:primer_sys}
\end{equation}

