
\section{ѕример}

«аданы системы дифференциальных уравнений, описывающих состо€ние некоторой модели до переключени€ и после:
\begin{equation}
\left\{
\begin{aligned}
			%\begin{aligned}
			&\left[
			\begin{aligned}
			    &\left\{
					    \begin{aligned}
					        &\dot x_1^{(1)} = x_2^{(1)}+v^{(1)}(t);\\
					        &\dot x_2^{(1)} = u(t)^{(1)};\\
					        &x(t_0^{(1)})\in\mathcal{X}_0=\{x\in \mathbb{R}^2:(x_1-2)^2+(x_2-1)^2\leqslant1\};\\
					    \end{aligned}
			    \right.\\
			    \\
			    &\left\{
					    \begin{aligned}
					        &\dot x_1^{(2)} = x_2^{(2)}+v^{(2)}(t);\\
					        &\dot x_2^{(2)} = -\gamma x_1 -\mu x_2 u^{(1)}(t);\\
					        &x(t_0^{(2)})\in\mathcal{X}[\tau,t_0^{(1)}]\cap\mathcal{H},t_0^{(2)}=\tau;
					    \end{aligned}
					\right.\\
			\end{aligned}
			\right.\\
			\\
			&\mathcal{H}=\{x\in \mathbb{R}^2: x_1=0\}\mbox{--- гиперплоскость};\\
			&u(\cdot)\in[-\alpha_1,\alpha_2],\;v(\cdot)\in[-\beta_1,\beta_2];\\
			&\gamma,\mu,\alpha_1,\alpha_2,\beta_1,\beta_2 > 0\mbox{--- некоторые константы};\\
			&\tau \mbox{ --- момент  переключени€}.\\
\end{aligned}
\right.
\label{eq:primer_sys}
\end{equation}

ƒл€ поиска множества $\mathcal{X}[t,t_0]$ будем использовать функцию цены
\begin{equation}
\label{eq:value_f}
V(t,x)=\min_{u(\cdot)}\max_{v(\cdot)}d^2(x_0,\mathcal{X}_0)\mid_{x(t)=x},
\end{equation}

где $d(x_0,\mathcal{X}_0)$ - рассто€ние между точкой $x_0$ и множеством $\mathcal{X}_0$, определ€емое метрикой
$d(x,\mathcal{X})=\min_{y\in\mathcal{X}} \|x-y\|^2$
$$
\phi^{*}(x^{*})=\sup_{x}( \langle x,x^* \rangle -  d^2(x,\mathcal{X}_0)) = \rho(\ell\mid\mathcal{X}_0)+\frac{\ell^2}{4}
$$

тогда

$$
V(t,x)=\min_{u(\cdot)}\max_{v(\cdot)}\sup_{\ell}\left(\langle \ell,x_0\rangle - \rho(\ell\mid\mathcal{X}_0)-\frac{\ell^2}{4}\right);
$$

¬ыпишем теперь решение $x(t,t_0,x_0)$  дл€ первой системы из \eqref{eq:primer_sys}
$$
\begin{aligned}
x^{(1)}_2(t,t_0)&=x^{(1)}_{02}+\int\limits_{t_0^{(1)}}^{t}u^{(1)}(s)\,ds;\\
x^{(1)}_1(t,t_0)&=x^{(1)}_{01}+\int\limits_{t_0^{(1)}}^{t}\left( \int\limits_{t_0^{(1)}}^{\tau}u^{(1)}(s)\,ds + x^{(1)}_{02} \right)\,d\tau + \int\limits_{t_0^{(1)}}^{t} v^{(1)}(s)\, ds;\\
\end{aligned}
$$

–ешение дл€ второй системы 
$$
x^{(2)}(t,t^{(2)}_0)=G(t,t^{(2)}_0)x^{(2)}_0 + \int\limits_{\tau}^t G(t,s)\left(Bu(s)+Cv(s)\right)\,ds,
$$
где $G(t,s)=e^{A(t-s)},\,B={\begin{pmatrix} 0 \\ 1\end{pmatrix}},\,C={\begin{pmatrix} 1 \\ 0\end{pmatrix}},A={
\begin{pmatrix}
0 & 1\\
-\gamma & -\mu
\end{pmatrix}} $

“ак как мы оцениваем рассто€ние от максимально близкой к множеству $\mathcal{X}_0$ начальную точку $x_0$ дл€ точки $x$, выпишем выражение дл€ $x_0$
$$
x_0(t_0,\tau,t_1,u,v,x)=G_1(t_0,\tau)G_2(\tau,t_1)x+\int\limits_{t_1}^
$$